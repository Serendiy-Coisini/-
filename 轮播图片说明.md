# 轮播图片添加说明

## 📍 图片存放位置

将你的图片放在以下目录：

```
C:\Users\zhong\IdeaProjects\campus-retail-system\src\main\resources\static\img\
```

> **注意**：如果 `img` 目录不存在，请先创建该目录。

## 📝 图片命名规则

**登录页面轮播图**（左侧全屏显示）：
- `login1.jpg` 或 `login1.png` - 第一张轮播图
- `login2.jpg` 或 `login2.png` - 第二张轮播图  
- `login3.jpg` 或 `login3.png` - 第三张轮播图

**首页横幅图**（顶部横幅显示）：
- `home1.jpg` 或 `home1.png` - 首页横幅图片（仅需1张）

> **提示**：
> - 登录页面使用轮播功能（3张图片），首页使用单张图片显示
> - 登录页面和首页现在使用**完全独立**的图片资源
> - 如果使用 PNG 格式，文件名应为 `login1.png`、`home1.png` 等
> - 首页图片会完整显示（不裁剪），高度固定为150px

## 🖼️ 支持的图片格式

- **JPG / JPEG**（推荐）
- **PNG**
- 其他浏览器支持的图片格式（GIF、WebP 等）

## 📐 图片尺寸建议

### 登录页面轮播图
- **推荐尺寸**：宽度 1920px，高度 1080px（16:9 比例）
- **最小尺寸**：宽度 1200px，高度 600px
- **用途**：左侧全屏显示

### 首页横幅图
- **显示方式**：完整显示（不裁剪），图片会按比例缩放以适应150px高度
- **推荐尺寸**：宽度 1920px，高度 150px（或按比例缩放）
- **显示高度**：固定为 150px
- **用途**：顶部横幅显示，图片会完整压缩显示，不会截取中间部分

### 文件大小建议
- 每张图片建议不超过 **2MB**，以保证页面加载速度
- 可以使用图片压缩工具（如 TinyPNG）压缩图片

## 🎨 图片内容建议

- 校园风景照片
- 校园建筑照片
- 校园活动照片
- 其他与校园相关的图片

## ⚙️ 详细操作步骤

### 步骤 1：准备图片

1. 选择3张你喜欢的校园风景图片
2. 确保图片清晰、美观
3. 如果图片太大，可以使用图片编辑软件调整尺寸

### 步骤 2：放置图片

**方法一：使用文件资源管理器**

1. 打开文件资源管理器（Windows + E）
2. 导航到：`C:\Users\zhong\IdeaProjects\campus-retail-system\src\main\resources\static\`
3. 如果 `img` 文件夹不存在，右键 → 新建 → 文件夹，命名为 `img`
4. 将登录页面的3张图片复制到 `img` 文件夹，重命名为：`login1.jpg`、`login2.jpg`、`login3.jpg`
5. 将首页的1张图片复制到 `img` 文件夹，重命名为：`home1.jpg`

**方法二：使用 IntelliJ IDEA**

1. 在 IDEA 项目左侧文件树中找到：`src/main/resources/static/`
2. 如果 `img` 文件夹不存在，右键 `static` → New → Directory，输入 `img`
3. 将图片文件拖拽到 `img` 文件夹
4. 右键图片文件 → Refactor → Rename，改为对应的文件名（`login1.jpg`、`home1.jpg` 等）

### 步骤 3：验证图片

1. 启动项目
2. 访问登录页面：`http://localhost:8080/login`
   - 查看左侧是否显示登录页面的轮播图片（`login1.jpg`、`login2.jpg`、`login3.jpg`）
3. 登录后查看首页：`http://localhost:8080/`
   - 查看顶部是否显示首页的横幅图片（`home1.jpg`），图片应完整显示且高度为150px

## ⚠️ 注意事项

1. **图片不存在**：如果图片文件不存在，页面会自动隐藏该图片，不影响系统功能
2. **图片格式**：如果使用 PNG 格式，需要确保 HTML 中的图片路径也是 `.png`（当前代码使用 `.jpg`）
3. **图片路径**：图片路径是相对于 `static` 目录的，在 HTML 中使用 `/img/login1.jpg` 或 `/img/home1.jpg` 即可访问
4. **轮播功能**：仅登录页面有轮播功能（每 4 秒自动切换，可以点击下方的小圆点手动切换），首页为单张图片显示
5. **首页图片显示**：首页图片会完整显示（不裁剪），高度固定为150px，图片会按比例缩放以适应容器
6. **功能分离**：登录页面和首页使用不同的图片资源和独立的代码，互不影响

## 🔧 修改轮播图数量（高级）

如果需要添加更多轮播图（超过3张）：

### 1. 修改登录页面（login.html）

找到以下代码：
```html
<div class="carousel-item active"><img src="/img/login1.jpg" alt="登录背景1"></div>
<div class="carousel-item"><img src="/img/login2.jpg" alt="登录背景2"></div>
<div class="carousel-item"><img src="/img/login3.jpg" alt="登录背景3"></div>
```

添加更多：
```html
<div class="carousel-item"><img src="/img/login4.jpg" alt="登录背景4"></div>
```

同时添加对应的圆点：
```html
<span class="dot" onclick="loginCarousel.showSlide(3)"></span>
```

### 2. 修改首页（index.html）

首页目前为单张图片显示，如需更换图片，只需修改图片路径：
```html
<div class="banner">
    <img src="/img/home1.jpg" alt="首页横幅" onerror="this.onerror=null; this.src='/img/placeholder.svg';">
</div>
```


## ❓ 常见问题
### Q1：图片不显示怎么办？
检查以下几点：
- 图片文件是否放在正确的位置：`src/main/resources/static/img/`
- 登录页面图片文件名是否正确：`login1.jpg`、`login2.jpg`、`login3.jpg`
- 首页图片文件名是否正确：`home1.jpg`
- 图片格式是否正确（JPG 或 PNG）
- 重启项目后再次访问

### Q2：可以使用 PNG 格式吗？
可以，但需要修改 HTML 文件中的图片路径：
- 登录页面：将 `login1.jpg` 改为 `login1.png`（在 `login.html` 中修改）
- 首页：将 `home1.jpg` 改为 `home1.png`（在 `index.html` 中修改）

图片太大导致页面加载慢？
使用图片压缩工具压缩图片：
- 在线工具：TinyPNG（https://tinypng.com/）
- 本地工具：Photoshop、GIMP 等
- 目标：每张图片控制在 2MB 以内
